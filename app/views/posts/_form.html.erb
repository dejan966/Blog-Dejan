<%= form_with(model: post) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">

    <%= form.text_field :title, required:true, placeholder:"Title", class:"form-control" %>
  </div>
  <br/>
  <div class="field">

    <%= form.text_area :body, required:true, placeholder:"Body", class:"form-control" %>
  </div>
<br/>
  <div class="actions">
    <%= form.submit "Create Post", class:"btn btn-lg btn-primary btn-block text-uppercase" %>
  </div>
  <br/>
<% end %>
